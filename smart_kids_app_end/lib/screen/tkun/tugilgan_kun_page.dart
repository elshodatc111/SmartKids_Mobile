import 'package:flutter/material.dart';
class TugilganKunPage extends StatelessWidget {
  const TugilganKunPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('tkun'),),
    );
  }
}
